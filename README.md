# Intraday momentum on SPY: an independent replication and cost analysis

An independent replication and extension of
[`blackswan-quants/intraday-momentum`](https://github.com/blackswan-quants/intraday-momentum),
a set of five intraday momentum strategies on SPY built on QuantConnect/LEAN and described in the
accompanying paper by BlackSwan Quants.

This repository re-runs the five published backtests, checks them against the authors' own result
files, adds fifty-three backtests of its own (matched benchmarks, two constructed configurations,
five slippage levels for every strategy), and asks what the numbers allow one to conclude. Every
number in the report is regenerated by a script from the raw backtest data; nothing is typed by
hand.

**Read the report:** [`report.pdf`](report.pdf) (56 pages) or [`report.md`](report.md).

## Abstract

<!-- abstract -->
`blackswan-quants/intraday-momentum` implements five variants of an intraday momentum strategy on
SPY and presents them as a progression that culminates in Strategy 4, which the paper describes as
Pareto-dominant. This report re-runs the five published backtests, checks them against the
authors' own result files, adds fifty-three more, and reaches four conclusions.

First, there is a signal. A benchmark with no signal at all, on the same instrument, with the same
leverage rule and the same trading window, loses 36.1% over the period in which the strategy gains
271.4%, and it loses in both directions: 30.7% with the position reversed. Aligning the
benchmark's entry time with the strategy's own removes a further 17.8 points from the benchmark:
the opening half-hour, which the thirty-minute cadence excludes by construction, is worth that much
to a passive leveraged position in this sample. The edge is more sensitive to execution costs than
its size suggests, however. Reconstructing the effect of transaction costs in closed form from a
single backtest, the 271.4% falls to zero at 2.2 basis points of slippage per trade, while
leveraged buy-and-hold at the same leverage survives up to 218.

Second, the comparison on raw return is the less informative one. The strategy's 271% sits below
the 350% of leveraged buy-and-hold, but the gap is about the size of the margin interest that the
backtest does not charge, so it does not support an argument in either direction. The rest of the
comparison does hold: a beta of -0.05 and a quarter of the drawdown, 9.0% vs. 40.2%, because a
strategy that is flat every night does not touch an equity premium that, over this window, lives
almost entirely overnight.

Third, the persistence gate does not appear to be a standalone source of edge. It offsets a cost
that the five-minute exit check introduces, and the cell that separates the two effects was already
in the repository, in Strategy 1, although it had not been used for that purpose.

Fourth, the best of the fourteen configurations examined is not in the repository, but it is one
line away from Strategy 4 and its lead is small. Adding to Strategy 4 an entry condition that is
present in the code of Strategy 2 but absent from both published descriptions gives S4′, which
closes with 3.4% more capital over eight years, about 0.4% per year, on the strength of the 22 to
26 trades the condition blocks, at the price of a slightly deeper drawdown; whether that lead
survives outside this sample is not tested here. Corrected for selection across fourteen trials
with the Deflated Sharpe Ratio, S4′ remains distinguishable from search noise, although the test
is against a zero edge, not against Strategy 4. That check matters because the one significance
statistic the paper reports, QuantConnect's PSR, no longer reproduces on the platform today.
<!-- /abstract -->

## Two tables from the report

**T1.** The five published backtests, authors' result files vs. reproduced today (`tight`
variant, 0 bps). Everything reproduces except one platform statistic, discussed in §7.

<!-- table:T1 -->
| | source | return | Sharpe | drawdown | win rate | orders | PSR | turnover |
|---|---|---|---|---|---|---|---|---|
| S0 | authors | 196.232% | 0.835 | 11.2% | 40% | 3594 | 61.589% | 222.35% |
| | reproduced | **196.125%** | **0.835** | **11.2%** | **40%** | **3594** | **21.510%** | **222.36%** |
| S1 | authors | 177.757% | 0.842 | 10.1% | 32% | 4900 | 67.736% | 303.52% |
| | reproduced | **177.468%** | **0.841** | **10.1%** | **32%** | **4900** | **21.982%** | **303.52%** |
| S2 | authors | 227.320% | 0.948 | 8.7% | 40% | 3450 | 75.319% | 213.01% |
| | reproduced | **227.196%** | **0.947** | **8.7%** | **40%** | **3450** | **33.461%** | **213.01%** |
| S3 | authors | 252.248% | 1.012 | 9.3% | 40% | 3550 | 81.164% | 219.91% |
| | reproduced | **252.101%** | **1.011** | **9.3%** | **40%** | **3550** | **41.219%** | **219.91%** |
| S4 | authors | 259.235% | 1.036 | 8.4% | 40% | 3388 | 83.439% | 209.40% |
| | reproduced | **259.177%** | **1.036** | **8.4%** | **40%** | **3388** | **44.342%** | **209.40%** |

Authors: `stats/strat{s}_8y.json`, the backtest output preserved in the repository. Reproduced: the same code re-run today. Orders, Sharpe, drawdown, win rate and turnover agree within rounding on all five rows; the PSR differs by 39.1 to 45.8 points (41.3 on average). See §1 and §7.
<!-- /table -->

**T13.** The best configuration examined, S4′, vs. the best two in the repository, under
additional slippage of 0, 0.25 and 0.5 bps per fill. S4′ is Strategy 4 plus the VWAP entry
condition that Strategy 2 already contains; it leads by about 0.4% per year over eight years
and does not dominate Strategy 4 on drawdown (§6).

<!-- table:T13 -->
|  | 0 bps | 0.25 bps | 0.5 bps | $b^*$ (bps) | drawdown | Sharpe |
|---|---|---|---|---|---|---|
| S4′ tight | 271.4 | 219.4 | 174.8 | 2.178 | 9.0% | 1.075 |
| S4′ loose | 258.6 | 215.0 | 176.7 | 2.462 | 8.9% | 0.970 |
| S2 loose | 259.7 | 214.2 | 174.6 | 2.372 | 9.6% | 0.999 |
| S4 tight | 259.2 | 208.2 | 164.4 | 2.088 | 8.4% | 1.036 |

The 0.25 and 0.5 bps columns of S2′ and S4′ are **predicted** by the model of T6, not measured: for these two cells only the 0 bps run exists. The lead of S4′ tight over S4 tight is +0.03339 in log return over eight years (T8), that is, 3.4% more final capital, about 0.4% per year.
<!-- /table -->

## What this report is

A replication study. Where the paper, the README and the code describe the same thing
differently, the report records the difference and shows the measurement; where a number does not
reproduce, it says why. The original work made the analysis possible by publishing its code and
its result files, which is why it could be replicated at all.

## Repository layout

```
report.md                       the assembled report (generated by assemble.py)
report.pdf                      the same, typeset for reading
report_plain.pdf                the same, from build_pdf.py (pandoc + xelatex); generated locally,
                                not committed
sections/                       the report, one file per section
  00_abstract.md ... 08_limitations.md, 09_references.md
  appendix_a_deflated_sharpe_ratio.md
tables.md                       every table cited in the report (generated by build_tables.py)
supplementary.md                blocks that are not tables of the report but produce numbers cited
                                in it: T14, short exposure and overnight carry (§8), the provenance
                                of the authors' backtests (§7), the opening-window threshold (§8),
                                every figure of Appendix A
figures/                        F1, F2, F3 (generated by make_figures.py)
data/
  backtest_data_strategies.csv  50 backtests: S0 to S4, two exit variants, five slippage levels
  backtest_data_prime.csv       4 backtests: the constructed cells S2′ and S4′
  backtest_data_benchmarks.csv  4 backtests: the matched benchmarks
build_tables.py                 data/ -> tables.md, supplementary.md
make_figures.py                 data/ -> figures/
assemble.py                     sections + tables + figures -> report.md (and refreshes this README)
build_pdf.py                    report.md -> report.pdf (pandoc + xelatex, settings in pdf/meta.yaml)
benchmark_vol_targeted.py       the QuantConnect algorithm used for the benchmarks
instrumentation/
  equity_moments.py             the five accumulators behind Appendix A, as a drop-in snippet
LICENSE                         MIT, for the code
```

## Reproducing the numbers

Requirements: Python 3.10 or later with `pandas`, `numpy`, `matplotlib` (`pip install -r
requirements.txt`). For the PDF, `pandoc` and a LaTeX engine (`xelatex`).

```bash
python build_tables.py     # data/ -> tables.md, supplementary.md   (--all for two extra diagnostics)
python make_figures.py     # data/ -> figures/
python assemble.py         # sections + tables + figures -> report.md; refreshes the README tables
python build_pdf.py        # report.md -> report_plain.pdf
```

`tables.md`, `supplementary.md`, `report.md` and the tables in this README are generated files:
edit the sections or the data, not the output.

Five blocks read files that live in the original repository, the authors' backtest output
`stats/strat{0..4}_8y.json` and their trade files `DayOfTheWeek/Trades_Strat{2,3,4}_8y.csv`: T1,
the day-of-the-week table of §5.1, and, in `supplementary.md`, T14, the short-exposure block cited
in §8 and the provenance of the authors' backtests cited in §7. To regenerate them, clone the
original repository next to `build_tables.py` (the script also looks one directory up, and honours
`$INTRADAY_MOMENTUM_REPO`):

```bash
git clone https://github.com/blackswan-quants/intraday-momentum
```

Without the clone, `build_tables.py` skips those five blocks with a note and produces everything
else.

`report.pdf` is typeset separately from the same `report.md` and is the version meant for reading;
`build_pdf.py` produces `report_plain.pdf`, the build anyone can reproduce here with pandoc alone.
The text of the two is the same.

## Where the raw data come from

All 58 backtests were run on QuantConnect (LEAN, Free plan) in 2026. The strategy runs use the
repository's code unchanged except for the slippage setting and, for the constructed cells S2′
and S4′, the one-line change to the VWAP entry condition described in §4.1 of the report; the
strategy files are therefore not duplicated here. The benchmark runs use
`benchmark_vol_targeted.py`. The Free plan offers no API access, so the statistics were
transcribed from the platform's results panel into the CSV files; the accumulator values that the
cost decomposition (§2) and the Deflated Sharpe Ratio (Appendix A) depend on are printed by the
algorithms as runtime statistics and are recorded in `build_tables.py` and in the benchmark CSV.
`instrumentation/equity_moments.py` is the snippet that produced the Appendix A accumulators.

The QuantConnect platform has changed since the paper was published: one reported statistic
(the Probabilistic Sharpe Ratio) no longer reproduces on the same code, while every other
statistic does. The report discusses this in §7 and Appendix A.

## Open items

Things the report names as limits and does not do. Data collection for this version is closed;
each of these would need new runs or new instrumentation.

- **Temporal stability.** All fourteen configurations are measured on a single eight-year window.
  A rolling-window or sub-period analysis is the first check both this report and the paper lack
  (§8).
- **Uncertainty on the S4′ lead.** The advantage of S4′ over Strategy 4 rests on the 22 to 26
  trades the VWAP entry condition blocks. A bootstrap over those trades would put an interval
  around the +0.033 of log return (§6).
- **Holding-time distribution.** The statement that the `tight` exit threshold typically binds
  early in the life of a position is a geometric argument, not a measurement; a histogram of
  holding times would settle it (§4.1, §8).
- **The slippage range.** The 0.25 to 0.5 bps per fill used as a plausible level of additional
  slippage is a working assumption for a liquid ETF, not a calibrated figure; the conclusion that
  `loose` wins at realistic costs depends on it, the break-even levels do not (§4.5, §8).
- **The missing cell.** Band + VWAP without EMA, the one cell of the 2×2×2 design that was not
  run, is also the one the design points to as a possible candidate to beat S4′ (§8).

## License

The code in this repository is released under the MIT license (`LICENSE`). The text of the report,
`report.md` and `report.pdf`, is released under
[CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): reuse it with attribution.

The original repository, `blackswan-quants/intraday-momentum`, declares the MIT license in its
README. Nothing of theirs is redistributed here: the two tables and three supplementary blocks that
read their files do so from a local clone, which this repository does not contain.

## Author

Francesco Nicolò, second-year student in Mathematical Engineering at Politecnico di Milano.

## Acknowledgments

BlackSwan Quants, for publishing the strategies, the paper and the backtest output files that
made a replication possible.
